<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Article Index</title>
</head>
<body>
  <div class="articles-table">
    <table class="table table-dark">
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th>By</th>
        <th>Created At</th>
        <th>Updated At</th>
        <th>Actions</th>
      </tr>

      <% @articles.each do |article| %>
        <tr>
          <td><%= link_to article.title, article %></td>
          <td><%= link_to truncate(article.description, length: 100), article %></td>
          <td><%= article.user ? article.user.username : "Unknown" %></td>
          <td><%= time_ago_in_words(article.created_at) %> ago</td>
          <td><%= time_ago_in_words(article.updated_at) %> ago</td>
          <td><%= link_to "Edit", edit_article_path(article) %></td>
          <td><%= link_to "Show", article_path(article) %></td>
          <td><%= link_to "Delete", article, method: :delete, data: { confirm: "Are you sure?" } %></td>
        </tr>
      <% end %>
    </table>
  </div>
  <div class="mobile-view-articles">
    <% @articles.each do |article| %>
      <div class="card" >
        <div class="card-body">
          <h5 class="card-title"><%= link_to article.title, article %></h5>
          <h6 class="card-subtitle mb-2 text-muted">By: <%= article.user ? article.user.username : "Unknown" %></h6>
          <h6 class="card-subtitle mb-2 text-muted"><small>Created: <%= time_ago_in_words(article.created_at) %> ago</small> | <small>Updated: <%= time_ago_in_words(article.updated_at) %> ago</small></h6>
          <p class="card-text"><%= truncate(article.description, length: 100) %></p>
          <%= link_to "Edit", edit_article_path(article), class: "card-link btn-sm btn-primary" %>
          <%= link_to "Show", article_path(article), class: "card-link btn-sm btn-success" %>
          <%= link_to "Delete", article, method: :delete, data: { confirm: "Are you sure?" }, class: "card-link btn-sm btn-danger" %>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>