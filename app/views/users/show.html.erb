<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <div class="users-container">
  <h1 class="text-center mt-4"> Welcome <%= @user.username %>'s Page</h1>
  <div class="row justify-content-center">
    <div class="col-md-4 col-md-offset-4 center">
      <%= gravatar_for @user, size: 200 %>
    </div>
  </div>
  <% if logged_in? && current_user.username == @user.username %>
    <div class="user-show-btn-container">
      <%= link_to "Edit your Profile", edit_user_path(@user), class: "btn btn-outline-warning user-show-edit-btn" %>
    </div>
  <% end %>
  <h4 class="user-show-h4">
    <%= @user.username %>'s articles
  </h4>
  <div class="user-articles align-self-center">
    <div class="digg_pagination mb-4">
       <%= will_paginate @articles %>
    </div>
    <%= render "articles/articles", articles: @articles %>
    <div class="digg_pagination mb-4 mt-4">
       <%= will_paginate @articles %>
    </div>
  </div>
  </div>
</body>
</html>